import 'package:flutter/material.dart';

class TasbihDetails extends StatelessWidget {
  const TasbihDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('This is Tasbih Details'),
    );
  }
}