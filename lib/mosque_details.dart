import 'package:flutter/material.dart';

class MosqueDetails extends StatelessWidget {
  const MosqueDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('This is Mosque Details'),
    );
  }
}